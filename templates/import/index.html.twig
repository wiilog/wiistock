{% extends 'layout.html.twig' %}

{% block title %}Paramétrage / Import{% endblock %}

{% block page_content %}

    {% block page_filter %}
        {% include 'filters/filtersImport.html.twig' with {
            'page': 'PAGE_IMPORT',
            'tableSelector': '#tableImport'
        } %}
    {% endblock %}
    <div class="col-12">
        <h1>Import</h1>
    </div>
    <div class="col-12">
        {% if hasRight(constant('App\\Entity\\Menu::PARAM'), constant('App\\Entity\\Action::DISPLAY_IMPORT')) %}
            <div class="btn btn-primary" onclick="displayFirstModal()">
                <span class="fa fa-plus mr-2"></span>Nouvel import
            </div>
        {% endif %}
    </div>
    <div class="col-12">
        <table id="tableImport" class="table table-striped table-bordered w-100">
        </table>
    </div>
    {% include 'import/modalNewImport.html.twig' %}
    {% include 'import/modalConfirmCancel.html.twig' %}
    {% include 'import/modalLaunchPlanifiedImport.html.twig' %}

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/attachments.js') }}?{{ date().timestamp }}"></script>
    <script src="{{ asset('js/import.js') }}?{{ date().timestamp }}"></script>
{% endblock %}

