{% extends 'layout.html.twig' %}

{% block classbody %}
style='background-image: url({{asset('img/GTXBackAccueil2red.png')}})'
class='accueil-back'
{% endblock %}

{% block page_content %}
<div class="row ">
        <div class="align-self-end col">
            <div class="text-center offset-9 col-1  alerte">
                <h2>Alertes de seuil</h2>
                <a href="{{path('alerte_index')}}" class="fa fa-warning fa-2x">
                    <div class="marker">{{nbAlerte}}</div>
                </a>
 
        </div>
    </div>
</div>
{% endblock %}