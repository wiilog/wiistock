<div class="d-flex">
    <div class="dropdown dropright {{ not hasRight(constant('App\\Entity\\Menu::TRACA'), constant('App\\Entity\\Action::DELETE')) ? 'd-none' }}">
        {% include 'datatable/dotsRow.html.twig' %}
        <div class="dropdown-menu dropdown-follow-gt pointer">
            <div data-id='{{ receipt_association.id }}'
                 data-target='#modalDeleteReceiptAssociation'
                 data-toggle='modal'
                 class='dropdown-item d-flex align-items-center'
                 onclick="deleteRow($(this), $('#modalDeleteReceiptAssociation'), $('#submitDeleteReceiptAssociation'))">
                <span class="wii-icon wii-icon-trash-black mr-2"></span>Supprimer
            </div>
        </div>
    </div>
</div>
