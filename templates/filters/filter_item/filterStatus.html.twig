<div class="col-auto mb-2 select-filter">
    <div class="select2-overflow">
        <div class="input-group w-100 cursor-default minw-250px">
            <select class="form-control data select2 filter-select2" name="statut" id="statut" multiple="multiple" {{ disabled }}>
                {% for statut in statuts %}
                    {% if filterStatus is defined and statut.nom == filterStatus %}
                        {% set selected = 'selected' %}
                    {% else %}
                        {% set selected = '' %}
                    {% endif %}
                    <option class="d-none" value="{{ statut.id }}" {{ selected }}>{{ statut.nom }}</option>
                {% endfor %}
            </select>
        </div>
        <input hidden id="filterStatus" value="{{ filterStatus is defined ? filterStatus : '' }}">
    </div>
</div>
