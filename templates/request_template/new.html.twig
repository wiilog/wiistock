<div class="modal fade" id="modalNewRequestTemplate" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Nouveau modèle de demande</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body modal-input container-fluid">
                <div class="form-group">
                    <label>Nom du modèle*</label>
                    <input type="text" class="form-control data needed" name="name"/>
                </div>

                <div class="form-group">
                    <label>Type de demande*</label>
                    <select name="type" class="form-control data needed w-100" data-s2>
                        <option value="{{ constant('App\\Entity\\IOT\\RequestTemplate::TYPE_HANDLING') }}">Service</option>
                        <option value="{{ constant('App\\Entity\\IOT\\RequestTemplate::TYPE_DELIVERY') }}">Livraison</option>
                        <option value="{{ constant('App\\Entity\\IOT\\RequestTemplate::TYPE_COLLECT') }}">Collecte</option>
                    </select>
                </div>

                {# TODO 4455: handling request template #}
                {# TODO 4456: handling request template #}
                {# TODO 4457: handling request template #}
            </div>
            <div class="error-msg"></div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Annuler</button>
                <div id="submitNewRequestTemplate" class="btn btn-primary">Enregistrer</div>
            </div>
        </div>
    </div>
</div>
