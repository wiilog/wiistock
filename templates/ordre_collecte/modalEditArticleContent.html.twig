{% if modifiable %}
<div class="mb-3 form-group">
    <label for="quantite">Quantité*</label>
    <input type="number" class="data form-control needed" name="quantite" id="quantite"
        value="{{ ligneArticle.quantite }}" min="1">
</div>
{% else %}
<div class="mb-3 form-group">
    Cette ligne concerne une référence CEA gérée par article. Il n'est donc pas possible de modifier la quantité interne de ce dernier.
</div>
{% endif %}