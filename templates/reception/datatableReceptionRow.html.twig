<div class="d-flex">
    {% if hasRight(constant('App\\Entity\\Menu::ORDRE'), constant('App\\Entity\\Action::DISPLAY_RECE')) %}
        <a href='{{ path('reception_show', {id: reception.id}) }}'>
            <div class="btn btn-col-action">
                <i class='fas fa-eye'></i>
            </div>
        </a>
    {% endif %}
</div>
