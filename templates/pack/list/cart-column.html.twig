{% set articles = pack.childArticles | length %}
{% if articles %}
    <div class="d-flex">
        <span
            class="wii-icon-cart wii-icon mr-2 add-cart"
            data-id="{{ pack.id }}">
        </span>
    </div>
{% endif %}
