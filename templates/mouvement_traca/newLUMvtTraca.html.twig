<div class="form-mass-mvt-container row">
    <div class="form-group col-12 pack-selector">
        <label class="wii-field-name">{{ trans('Traçabilité', 'Général', 'Unité logistique') }}*</label>
        <input class="form-control data needed w-100" name="colis" data-fill-location/>
    </div>
    <div class="form-group col-12">
        <label class="wii-field-name">{{ trans('Traçabilité', 'Mouvements', 'Article(s)') }}*</label>
        <select class="form-control data needed select2-free select2-fit-content w-100" name="articles"
                data-s2="availableArticle" multiple>
        </select>
    </div>
    <div class="form-group col-12">
        <label class="wii-field-name">{{ trans('Traçabilité', 'Général', 'Emplacement') }}*</label>
        <select name="emplacement"
                class="form-control ajax-autocomplete-location data needed w-100"
                title="Emplacement de dépose">
        </select>
    </div>
</div>
