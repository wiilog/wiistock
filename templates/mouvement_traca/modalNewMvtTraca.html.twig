<form method="" class="modal fade" id="modalNewMvtTraca" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">

            <div class="modal-header">
                <h5 class="modal-title">Nouveau mouvement</h5>

                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>

            <div class="modal-body modal-input container-fluid">
                <div class="row">
                    <div class="form-group col-6">
                        <label for="datetime">Date*</label>
                        <input type="datetime-local" class="form-control data datetime needed no-clear" id="datetime"
                               name="datetime">
                    </div>

                    <div class="form-group col-6 w-100">
                        <label for="operator mtb-auto mr-2">Opérateur*</label>
                        <select name="operator" class="form-control ajax-autocomplete-user data needed operator no-clear">
                            <option value="{{ app.user.id }}">{{ app.user.username }}</option>>
                        </select>
                    </div>

                    <div class="form-group col-6">
                        <label for="colis">Colis*</label>
                        <input type="text" class="form-control data needed" id="colis" name="colis">
                    </div>

                    <div class="form-group col-6">
                        <label for="type">Action*</label>
                        <select class="form-control data needed" id="type" name="type">
                            <option disabled selected></option>
                            {% for statut in statuts %}
                                <option value="{{ statut.id }}">{{ statut.nom }}</option>
                            {% endfor %}
                        </select>
                    </div>

                    <div class="form-group col-12 w-100">
                        <label class="mtb-auto mr-2">Emplacement*</label>
                        <select name="emplacement" class="form-control ajax-autocompleteEmplacement data needed">
                            <option value=" "></option>>
                        </select>
                    </div>

                </div>

                <div class="row">
                    <div class="form-group col-12 h-fit-content">
                        <label for="commentaire">Commentaire</label>
                        <input id="commentaire" class="commentaire form-control data" name="commentaire" type="hidden">
                        <div class="editor-container-new" ></div>
                    </div>
                    <div class="form-group col-12">
                        <div>
                            {% include 'arrivage/attachements.html.twig' with {'isNew' : true} %}
                        </div>
                    </div>
                </div>
            </div>
            <div class="error-msg p-4"></div>
            <div class="modal-footer">
                <button type="button" class="btn" data-dismiss="modal">Annuler</button>
                <button id="submitNewMvtTraca" type="button" class="btn btn-success"
                    onclick="setCommentaire('.editor-container-new', quillNew)">Enregistrer
                </button>
            </div>
        </div>
    </div>
</form>