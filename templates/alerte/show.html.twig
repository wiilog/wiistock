{% extends 'layout.html.twig' %}

{% block title %}Alerte{% endblock %}

{% block page_content %}
    <h1>Alerte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Identifiant</th>
                <td>{{ alerte.id }}</td>
            </tr>
            <tr>
                <th>Utilisateur</th>
                <td>{{ alerte.AlerteUtilisateur }}</td>
            </tr>
            <tr>
                <th>Nom de l'alerte</th>
                <td>{{ alerte.AlerteNom }}</td>
            </tr>
            <tr>
                <th>Numéro d'alerte</th>
                <td>{{ alerte.AlerteNumero }}</td>
            </tr>
            <tr>
                <th>Niveau du seuil</th>
                <td>{{ alerte.AlerteSeuil }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn" href="{{ path('alerte_index') }}">Voir la liste</a>

    <a class="btn" href="{{ path('alerte_edit', {'id': alerte.id}) }}">Modifier</a>

    {{ include('alerte/_delete_form.html.twig') }}
{% endblock %}
