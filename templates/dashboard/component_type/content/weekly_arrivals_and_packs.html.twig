{% extends 'dashboard/component_type/content/base_form.html.twig' %}
{% block content %}
    <div class="row">
        <div class="form-group col-6">
            <label>Type(s) d'arrivage*</label>
            <div class="input-group flex-nowrap">
                <div class="flex-fill">
                    <select class="data form-control h-100 w-100 needed select2"
                            name="arrivalTypes"
                            multiple
                            title="Type(s) d'arrivage">
                        {% for type in arrivalTypes %}
                            <option value="{{ type.id }}" {{ type in values.arrivalTypes ? 'selected' }}>{{ type.label }}</option>
                        {% endfor %}
                    </select>
                </div>
                <div class="input-group-append" style="width:150px">
                    <button class="btn btn-primary select-all-arrival-types w-100" type="button">Tous les type</button>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="form-group col-6">
            <label>Statut(s)*</label>
            <div class="input-group flex-nowrap">
                <div class="flex-fill">
                    <select class="data form-control h-100 w-100 needed select2"
                            name="arrivalStatuses"
                            multiple
                            title="Statut(s)">
                        {% for status in arrivalStatuses %}
                            <option value="{{ status.id }}" {{ status in values.arrivalStatuses ? 'selected' }}>{{ status.nom }}</option>
                        {% endfor %}
                    </select>
                </div>
                <div class="input-group-append" style="width:150px">
                    <button class="btn btn-primary select-all-arrival-statuses w-100" type="button">Tous les statuts</button>
                </div>
            </div>
        </div>
    </div>

    <div class="form-group">
        <label class="switch">
            <input class="checkbox data needs-default"
                   type="checkbox"
                   name="displayPackNatures"
                {{ values.displayPackNatures ?? false ? 'checked' }}>
            <span class="slider round"></span>
        </label>
        <label>Montrer les natures de colis</label>
    </div>
{% endblock %}
