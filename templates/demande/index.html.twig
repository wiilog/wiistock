{% extends 'layout.html.twig' %}

{% block title %}Demandes de livraison{% endblock %}

{% block page_content %}
</div>
{% include 'filters/filters.html.twig' with {'btnName' : 'submitSearchDemandeLivraison'} %}
{#fermeture des filtres#}
{#ouverture d'un nouveau container pour aligner sur tableau#}

<div class="container-fluid">

    <h1>Demandes de livraison</h1>

    <div class="row">
        {% if hasRight(constant('App\\Entity\\Menu::DEM_LIVRAISON'), constant('App\\Entity\\Action::CREATE_EDIT')) %}
        <div href="" data-toggle="modal" data-target="#modalNewDemande" class="btn btn-primary"
            onclick='initNewLivraisonEditor("#modalNewDemande")'>
            <span class="fa fa-plus mr-2"></span>Nouvelle demande
        </div>
        {% endif %}
        {% if hasRight(constant('App\\Entity\\Menu::DEM_LIVRAISON'), constant('App\\Entity\\Action::EXPORT')) %}
        <div class="btn btn-primary" onclick='generateCSV()'>
            <span class="fa fa-plus mr-2"></span>Générer CSV
        </div>
        {% endif %}
    </div>
    <div class="row">
        <div class="error-msg"></div>
    </div>
    <div>
        <table id="table_demande" class="table table-striped table-bordered w-100">
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Demandeur</th>
                    <th>Numéro</th>
                    <th>Statut</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>



</div>
{% include "demande/modalNewDemande.html.twig" %}
{% include "demande/modalShowDemande.html.twig" %}

{% endblock %}

{% block javascript %}
<script>
    const id = 0;
</script>
<script src="{{ asset('js/demande-livraison.js')}}?{{ date().timestamp }}"></script>
{% endblock %}