{% extends 'layout.html.twig' %}

{% block title %}Demande de livraison{% endblock %}

{% block page_content %}

<!-- <form action="" class='row backgrey filter-wrapper'>
    <div class="input-group daterange">
        <div class="input-group-addon"> De </div>
        <input type="date" id='dateDebut' class='form-control' placeholder="Date de début">
        <div class="input-group-addon"> à </div>
        <input type="date" id='dateFin' class='form-control' placeholder="Date de fin">
    </div>
    <select type="text" id='statut' class="form-control basic-multiple-etat select2" multiple="multiple">
        {% for statut in statuts %}
        <option value={{statut.nom}}>{{statut.nom}}</option>
        {% endfor %}
    </select>
    <select type="text" id='utilisateur' class="form-control basic-multiple-etat select2" multiple="multiple">
        {% for utilisateur in utilisateurs %}
        <option value={{utilisateur.username}}>{{utilisateur.username}}</option>
        {% endfor %}

    </select>

    <button id='filter' class='button'><i class="fas fa-filter"></i> Filtrer</button>
</form> -->
<div class="row">
    <div class="barre-filtre col-12">
        <h1>Demande de livraison</h1>
        <div class='row btn pull-right'>
            <a href="" data-toggle="modal" data-target="#dataModalCenter" class="button btn-left">
                <span class="fa fa-plus"></span> Nouvelle demande
            </a>
        </div>
    </div>

    <div class="zone-actions col-12">
    </div>

    <div class="zone-affichage col-12">
        <table id="table_demande" class="table table-striped table-bordered" style="width:100%">
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Demandeur</th>
                    <th>Numéro</th>
                    <th>Statut</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>

    <div class="actions-secondaires col-12"></div>
</div>
{% include "demande/modalDemande.html.twig" %}

{% endblock %}

{% block javascript %}
<script>
    //var data = {'utilisateur': $('#utilisateur').val()};

var table = $('#table_demande').DataTable({
        order: [[ 0, "desc" ]],
        language: {
            "url": "//cdn.datatables.net/plug-ins/9dcbecd42ad/i18n/French.json"
        },
        ajax:{ 
            "url":"{{path('demande_api') }}",
            "type": "POST",
            //"data": function (d) {
            //    console.log($('#utilisateur').val());
            //    d.utilisteur = data['utilisateur'];
            //}
        },
        columns: [
        { "data": 'Date' },
        { "data": 'Demandeur' },
        { "data": 'Numéro' },
        { "data": 'Statut' },
        { "data": 'Actions' },
        ],
    });
    
    //$('#filtre').on('click', $.post( "{{path('demande_api')}}" , {'utilisateur': $('#utilisateur').val()},table.ajax.reload()));

    var dataModal = document.getElementById("dataModalCenter");
    var ButtonSubmit = document.getElementById("submitButton");
    InitialiserModal(dataModal, ButtonSubmit, "{{ path('creation_demande') }}");


    $('#filtre').ajaxSend()

</script>
{% endblock %}