<form method="" class="modal fade" id="modalNewDemande" role="dialog" aria-labelledby="dataModalCenterTitle"
      aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLongTitle">Créer une demande</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body modal-input container-fluid">
                <div class="form-group">
                    <label for="demandeur">Demandeur</label>
                    <select type="text" class="demandeur form-control data" name="demandeur" disabled>
                        <option value="{{ app.user.id }}">{{ app.user.username }}</option>
                    </select>
                </div>

                <div class="form-group w-100">
                    <label for="destination">Destination*</label>
                    <select name="destination" class="form-control data ajax-autocompleteEmplacement d-block needed" id="locationDemandeLivraison">
                    </select>
                    <input hidden id="locationDemandeLivraisonValue" data-id="{{ livraisonLocation ? livraisonLocation.id }}" data-text="{{ livraisonLocation ? livraisonLocation.text }}">
                </div>

                <div class="form-group">
                    <label for="type">Type*</label>
                    <select name="type" class="form-control data d-block type needed"
                            onclick="typeChoice($(this), '-new', $('#typeContentNew'))"
                            onchange="toggleRequiredChampsLibres($(this),'create')">
                        <option value="">Type à choisir...</option>
                        {% for type in typeChampsLibres %}
                            <option value="{{ type.typeId }}">{{ type.typeLabel }}</option>
                        {% endfor %}
                    </select>
                </div>

                <!-- CHAMPS LIBRES -->
                <div class="form-group col-12 blocVisible d-block" id='typeContentNew'>
                    {% for type in typeChampsLibres %}
                        <div class="d-none" id="{{ type.typeId }}-new">
                            <div class="row mt-2">
                                {% if type.champsLibres|length > 0 %}
                                    <p class="col-12 pl-0">Champs Libres :</p>
                                {% endif %}
                                {% for champ in type.champsLibres %}
                                    <div class="form-group col-6 pl-0">
                                        <label>
                                            {{ champ.label|capitalize }}
                                        </label>

                                        {% if champ.typage == constant('App\\Entity\\ChampLibre::TYPE_BOOL') %}
                                            {% set checked = (champ.defaultValue == 1) ? 'checked' : '' %}
                                            <label class="switch">
                                                <input type="checkbox" class="data checkbox" {{ checked }}
                                                       name="{{ champ.id ~ '-' ~ type.typeId }}">
                                                <span class="slider round"></span>
                                            </label>

                                        {% elseif champ.typage == 'number' %}
                                            <input type="number" id="{{ champ.id ~ 'create' }}"
                                                   class="form-control data"
                                                   value="{{ champ.defaultValue }}" name="{{ champ.id ~ '-' ~ type.typeId }}">

                                        {% elseif champ.typage == 'list' %}
                                            <select id="{{ champ.id ~ 'create' }}" class="form-control data"
                                                    name="{{ champ.id ~ '-' ~ type.typeId }}"
                                                    id="input">
                                                <option value="{{ champ.defaultValue }}">{{ champ.defaultValue }}
                                                </option>
                                                {% for elem in champ.elements %}
                                                    {% if champ.defaultValue != elem %}
                                                        <option value="{{ elem }}">{{ elem }}</option>
                                                    {% endif %}
                                                {% endfor %}
                                                {% if champ.defaultValue != "" %}
                                                    <option value=""></option>
                                                {% endif %}
                                            </select>
                                        {% elseif champ.typage == 'list multiple' %}
                                            <select id="{{ champ.id ~ 'create' }}" class="form-control data list-multiple"
                                                    name="{{ champ.id ~ '-' ~ type.typeId }}" multiple="multiple">
                                                {% for elem in champ.elements %}
                                                    <option value="{{ elem }}">{{ elem }}</option>
                                                {% endfor %}
                                            </select>
                                        {% elseif champ.typage == 'text' %}
                                            <input id="{{ champ.id ~ 'create' }}" class="form-control data" type="text"
                                                   value="{{ champ.defaultValue }}" name="{{ champ.id ~ '-' ~ type.typeId }}">

                                        {% elseif champ.typage == 'date' %}
                                            <input type="date" name="{{ champ.id ~ '-' ~ type.typeId }}" id="{{ champ.id ~ 'create' }}"
                                                   class="form-control data" value="{{ champ.defaultValue }}">
                                        {% elseif champ.typage == 'date' %}
                                            <input type="date" name="{{ champ.id ~ '-' ~ type.typeId }}" id="{{ champ.id ~ 'create' }}"
                                                   class="form-control data" value="{{ champ.defaultValue }}">
                                        {% elseif champ.typage == 'datetime' %}
                                            <input type="datetime-local" name="{{ champ.id ~ '-' ~ type.typeId }}"
                                                   id="{{ champ.id ~ 'create' }}"
                                                   class="form-control data" value="{{ champ.defaultValue }}">
                                        {% else %}
                                            <input disabled>

                                        {% endif %}
                                    </div>
                                {% endfor %}
                            </div>

                        </div>
                    {% endfor %}
                </div>

                <div class="form-group">
                    <label for="commentaire">Commentaire</label>
                    <input id='commentaire' class="commentaire form-control data" name="commentaire" type="hidden">
                    <div class="editor-container" id="editor-container-new">
                    </div>
                </div>
            </div>
            <div class="error-msg p-4"></div>
            <div class="modal-footer">
                <button type="button" class="btn" data-dismiss="modal">Annuler</button>
                <button id="submitNewDemande" type="button"
                        class="btn btn-success" onclick="setCommentaire('#editor-container-new')">Enregistrer
                </button>
            </div>
        </div>
    </div>
</form>
