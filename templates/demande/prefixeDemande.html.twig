{% extends 'layout.html.twig' %}

{% block title %}préfixage des noms de demandes{% endblock %}

{% block page_content %}


    <div class='row justify-content-center'>
        <form action="" class="col-3" id="dimsForm">
            <h1>Préfixage des noms de demandes</h1>
            <div class="form-group">
                <label for="typeDemande" id="typeDemandePrefixageDemande">Type de demande</label>
                <select name="typeDemande" class="form-control data d-block type needed">
                    <option value="">Type demande à choisir...</option>
                    //TODO gérer le cas de tous les types de demandes
{#                    {% for type in typeChampsLibres %}#}
{#                        <option value="{{type.typeId}}">{{type.typeLabel}}</option>#}
                        <option value="livraison">Demande de livraison</option>
{#                    {% endfor %}#}
                </select>
            </div>

            <div class="form-group">
                <label for="">Préfixe</label>
                <input type="text" class="form-control data needed" name="height" id="prefixeDemande"
                       value="{{prefixe_demande.prefix is defined ? prefixe_demande.prefix :'' }}">
            </div>
            <button type="button" class="btn btn-success" onclick="prefixDemand()">Enregistrer</button>
        </form>
    </div>

    {% include 'demande/modalPrefixageDemandeSet.html.twig' %}

{% endblock %}
{% block javascript %}
<script src="{{ asset('js/demande-livraison.js') }}?{{ date().timestamp }}"></script>
{% endblock %}