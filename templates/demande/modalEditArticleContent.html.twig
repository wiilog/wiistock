<div class="mb-3">
    <label for="quantite">Quantité</label>
    <input type="number" class="data form-control" name="quantite" id="quantite" value="{{ligneArticle.quantite}}"
        placeholder="Quantité" min="1" required="true">
</div>