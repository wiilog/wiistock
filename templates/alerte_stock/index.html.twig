{% extends 'layout.html.twig' %}

{% block title %}Paramétrage alertes stock{% endblock %}

{% block page_content %}
    </div>
{% include 'filters/filtersAlerts.html.twig' with {'btnName' : 'submitSearchAlerteStock'} %}
{#fermeture des filtres#}
{#ouverture d'un nouveau container pour aligner sur tableau#}

<div class="container-fluid">
<h1>Liste des alertes de stock</h1>

<div class='row'>
    <div class="col-4">
    {% if hasRight(constant('App\\Entity\\Menu::PARAM')) %}
        <a href="" data-toggle="modal" data-target="#modalNewAlerteStock"
            onclick="ajaxAutoRefArticleInit($('.ajax-autocomplete')),  ajaxAutoUserInit($('.ajax-autocomplete-user'))"
            class="btn btn-primary">
            <span class="fa fa-plus mr-2"></span>Nouvelle alerte
        </a>
    {% endif %}
    </div>
    <div class="offset-6 col-2">
        <label class="switch">
            <input class="checkbox" type="checkbox" onclick="filterActiveAlerts($(this))">
            <span class="slider round"></span>
        </label>
        <label>alertes actives</label>
    </div>
</div>

<table id="tableAlerteStock_id" class="table table-striped table-bordered w-100">
</table>

{% include "alerte_stock/modalDeleteAlerteStock.html.twig" %}

{% include "alerte_stock/modalEditAlerteStock.html.twig" %}

{% include "alerte_stock/modalNewAlerteStock.html.twig" %}

{% endblock %}

{% block javascript %}
<script src="{{ asset('js/alerte-stock.js') }}?{{ date().timestamp }}"></script>
{% endblock %}