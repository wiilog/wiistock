<div class="row">
    <div class="form-group col-10 inputColisClone clear-clone">
        <label for="colis">Colis*</label>
        <input type="text" id="colis" class="form-control data-array needed" name="colis" maxlength="24">
    </div>
    <div class="form-group col-2">
        <span class="btn btn-primary" id="btn-add-colis-acheminement" onclick="addInputColisClone($(this))"><i class="fa fa-plus"></i></span>
    </div>
</div>
<div class="form-group">
    <label for="demandeur">Demandeur*</label>
    <select type="text" class="demandeur form-control data needed" name="demandeur">
        <option value="" selected></option>
        {% for utilisateur in utilisateurs %}
            <option value="{{ utilisateur.id }}">{{ utilisateur.username }}</option>
        {% endfor %}
    </select>
</div>
<div class="form-group">
    <label for="destinataire">Destinataire*</label>
    <select type="text" class="destinataire form-control data needed" name="destinataire">
        <option value="" selected></option>
        {% for utilisateur in utilisateurs %}
            <option value="{{ utilisateur.id }}">{{ utilisateur.username }}</option>
        {% endfor %}
    </select>
</div>
<div class="form-group needed">
    <label for="prise">{{ 'acheminement.emplacement prise'|trans }}*</label>
    <input class="form-control data needed" name="prise" id="prise">
</div>
<div class="form-group needed">
    <label for="depose">{{ 'acheminement.emplacement dépose'|trans }}*</label>
    <input class="form-control data needed" name="depose" id="depose">
</div>
