{% if sensorCode is defined and sensorCode %}
    {% set sensorCode = '<strong>' ~ sensorCode ~ '</strong>' %}
    <i class="wii-icon wii-icon-pairing has-tooltip"
       title="Dernier capteur ayant remonté un message : {{ sensorCode }}"></i>
{% endif %}
