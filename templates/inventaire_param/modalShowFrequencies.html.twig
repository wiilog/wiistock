<form method="" class="modal fade" id="modalShowFrequencies" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            {% include('utils/modal/header.html.twig') with {
                title: "Liste des fréquences"
            } %}

            <div class="modal-body modal-input container-fluid">
                <table id="tableFrequencies" class="table w-100">
                </table>
            </div>
            <div class="modal-footer">
                <button id="submitNewFrequency" type="button" class="btn btn-success" data-dismiss="modal">Fermer</button>
            </div>
        </div>
    </div>
</form>

    {% include 'inventaire_param/modalEditFrequency.html.twig' %}
    {% include 'inventaire_param/modalDeleteFrequency.html.twig' %}
