{% extends 'base.html.twig' %}

{% block title %}Créer un compte{% endblock %}

{% block classbody %}
    style='background-image: url({{asset('img/GTXBackAccueil2red.png')}})'
    class='accueil-back'
{% endblock  %}

{% block body %}
    <div class="login-wrapper mt-3">
        <div class="row">
            <div class="box-login login-form box-shadow">
                <div class="login-header">
                    Créer un compte
                </div>
                <div class="form-signin">
                    {{ form_start(form) }}
                    {{ form_row(form.username, {'attr': {'autofocus' : true} }) }}
                    {{ form_row(form.email) }}
                    {{ form_row(form.plainPassword.first) }}
                    {{ form_row(form.plainPassword.second) }}

                    <button class="btn btn-primary float-left" type="submit">Enregistrer</button>
                    <a class="btn float-right" href="{{ url('login') }}">Retour connexion</a>
                    {{ form_row(form._token) }}
                    {{ form_end(form, {'render_rest' : false}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}