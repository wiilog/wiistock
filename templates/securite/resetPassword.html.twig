{% extends 'base.html.twig' %}

{% block title %}Mot de passe oublié{% endblock %}

{% block classbody %}
    style='background-image: url({{asset('img/GTXBackAccueil2red.png')}})'
    class='accueil-back'
{% endblock  %}

{% block body %}
    <div class="login-wrapper mt-3">
        <div class="row">
            <div class="box-login login-form box-shadow">
                <div class="login-header">
                    Votre adresse email ? 
                </div>
                <div class="form-signin">
                    {{ form_start(form) }}
                    {{ form_row(form.email) }}

                    <div class="d-flex justify-content-between">
                        <a class="btn" href="{{ url('login') }}">Retour connexion</a>
                        <button class="btn btn-primary float-left" type="submit">Valider</button>
                    </div>
                    {{ form_row(form._token) }}
                    {{ form_end(form, {'render_rest' : false}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}