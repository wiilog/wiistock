{% extends 'base.html.twig' %}

{% block title %}Mot de passe oublié{% endblock %}

{% block classbody %}
    class='new-body'
{% endblock %}

{% block body %}
    {% include 'header.html.twig' %}

<div class="alert alert-success text-center d-none">
    <span class="closebtn" onclick="this.parentElement.style.display='none';">&times;</span>
    <span class="confirm-msg"></span>
</div>

<div class="main-form-wrapper mt-3">
    <div class="row">
        <div class="box-login datatable-container shadow-lg">
            <div class="login-header">
                Mot de passe oublié
            </div>
            <div class="form-signin">
                <div class="form-group">
                    <label for="inputEmail" class="form-control-label">Adresse email</label>
                    <input type="text" id="inputEmail" name="_username" class="form-control" required="required"
                        autofocus="autofocus">
                </div>
                <div class="form-group float-right">
                    <a class="btn" href="{{ url('login') }}">Retour connexion</a>
                    <span class="btn btn-primary" onclick="checkIfUserExists()">Valider</span>
                </div>
                <div class="form-group float-left">
                    <p class="row error-msg"></p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/securite.js') }}"></script>
{% endblock %}
