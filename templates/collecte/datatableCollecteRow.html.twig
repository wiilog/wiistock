<div class="d-flex">
    {% if hasRight(constant('App\\Entity\\Menu::DEM_COLLECTE'), constant('App\\Entity\\Action::LIST')) %}
        <a href="{{url}}">
            <div class="btn btn-col-action">
                <i class='fas fa-eye'></i>
            </div>
        </a>
    {% endif %}
</div>