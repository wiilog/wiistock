{% extends 'base.html.twig' %}

{% block title %}Edit Zones{% endblock %}

{% block body %}
    <h1>Edit Zones</h1>

    {{ include('zones/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('zones_index') }}">back to list</a>

    {{ include('zones/_delete_form.html.twig') }}
{% endblock %}