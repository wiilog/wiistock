<div class="form-group">
    <label for="date-attendu">Label</label>
    <input type="text" class="form-control data" name="label" value="{{type.label}}" required="true">
</div>
<div class="form-group">
    <label for='category'>Catégorie</label>
    <select class="form-control select2 data" name='category' id='category'>
        <option value="{{type.category.id}}">{{type.category.label}}</option>
        {% for category in category %}
        <option value="{{ category.id }}">{{ category.label }}</option>
        {% endfor %}
    </select>
</div>