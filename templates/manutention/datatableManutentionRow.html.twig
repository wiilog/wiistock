<div class="d-flex">
    {% if hasRight(constant('App\\Entity\\Menu::DEM'), constant('App\\Entity\\Action::EDIT'))%}
        <div>
            <div class="btn btn-col-action" data-id='{{ manut.id }}' data-target='#modalEditManutention' data-toggle='modal'
                 onclick="editRow($(this), Routing.generate('manutention_edit_api', true), $('#modalEditManutention'), $('#submitEditManutention'), true, '.editor-container-edit', false, toggleManutQuill)">
                <i class='fas fa-pencil-alt'></i>
            </div>
        </div>
        {% if manut.statut.nom != constant('App\\Entity\\Manutention::STATUT_TRAITE') %}
            <div>
                <div class="btn btn-col-action" data-id='{{ manut.id }}' data-target='#modalDeleteManutention' data-toggle='modal'
                     onclick="deleteRow($(this), $('#modalDeleteManutention'), $('#submitDeleteManutention'))">
                    <i class='fas fa-trash'></i>
                </div>
            </div>
        {% endif %}
    {% else %}
        <div>
            <div class="btn btn-col-action" data-id='{{ manut.id }}' data-target='#modalShowManutention' data-toggle='modal'
                 onclick="showRow($(this), Routing.generate('manutention_show', true), $('#modalShowManutention'))">
                <i class='fas fa-eye'></i>
            </div>
        </div>
    {% endif %}
</div>