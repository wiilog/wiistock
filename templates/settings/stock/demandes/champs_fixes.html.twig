{% import 'form.html.twig' as form %}

{% include 'settings/fixed_field_elements.html.twig' with {
    modal: constant('App\\Entity\\FieldsParam::FIELD_CODE_RECEIVER_DEMANDE'),
    field: receiver.field,
    modalType: receiver.modalType,
    elements: receiver.elements,
} %}

{% include 'settings/fixed_field_elements.html.twig' with {
    modal: constant('App\\Entity\\FieldsParam::FIELD_CODE_TYPE_DEMANDE'),
    field: type.field,
    modalType: type.modalType,
    elements: type.elements,
} %}

{% include 'settings/fixed_field_elements.html.twig' with {
    modal: constant('App\\Entity\\FieldsParam::FIELD_CODE_DESTINATION_DEMANDE'),
    field: locationByType.field,
    modalType: locationByType.modalType,
    elements: locationByType.elements,
} %}

<div class="wii-section">
    <div class="row">
        <div class="col-12 mb-3 wii-describe">
            Contrairement aux champs libres, les champs fixes ne peuvent ni être supprimés, ni ajoutés. Vous pouvez
            paramétrer leur visibilité ou leur caractère obligatoire.
        </div>
    </div>
    <table id="table-demande-fixed-fields" class="table no-border-top w-100" data-table-processing="fixedFields">
        {% include 'datatable/main-header.html.twig' with {
            cells: 6
        } %}
    </table>
</div>
