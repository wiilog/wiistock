{% import 'form.html.twig' as form %}

<div class="modal fade" id="modal-edit-import" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
        <div class="modal-content">
            {% include 'utils/modal/header.html.twig' with {
                title: "Modifier un import",
                onManualDismiss: "deleteImport($(this))"
            } %}
           {# <div class="modal-header">
                <h5 class="modal-title">Modifier un import</h5>

                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true" onclick="deleteImport($(this))">&times;</span>
                </button>
            </div>#}

            <div class="modal-body modal-input container-fluid"></div>

            {% include 'utils/modal/footer.html.twig' with {
                elements: [
                    {input: true, classes: 'data', name: 'importId'},
                    {classes: 'btn btn-outline-secondary', dismiss: true, text: 'Annuler'},
                    {classes: 'btn btn-success', text: 'Sauvegarder', type: 'submit'},
                ],
            } %}
            {#<div class="modal-footer">
                <input hidden name="importId" class="data">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Annuler</button>
                <button type="button" class="btn btn-primary data submit-button" name="id">
                    Sauvegarder
                </button>
            </div>#}
        </div>
    </div>
</div>
