<form>
    <div class="row">
        <div class="form-group col-12">
            <label>Quantité commandée*</label>
            <input type="number" class="w-100 form-control data needed" name="orderedQuantity" min="1" title="Quantité commandée" value="{{ line.orderedQuantity }}">
        </div>
    </div>
    <div class="row">
        <div class="form-group col-12 col-md-6">
            <label>N° commande{{ fieldsParam|isFieldRequired('numCommande', 'mustToCreate') ? '*' }}</label>
            <input type="text"
                   class="w-100 form-control data {{ fieldsParam|isFieldRequired('numCommande', 'mustToCreate') ? 'needed' }}"
                   name="orderNumber"
                   title="N° commande"
                   value="{{ line.orderNumber }}">
        </div>
        <div class="form-group col-12 col-md-6">
            <label>Fournisseur{{ fieldsParam|isFieldRequired('fournisseur', 'mustToCreate') ? '*' }}</label>
            <div class="w-100">
                <select class="form-control data select2 ajax-autocomplete-fournisseur {{ fieldsParam|isFieldRequired('fournisseur', 'mustToCreate') ? 'needed' }}"
                        title="Fournisseur"
                        name="supplier">
                    {% if line.supplier %}
                        <option value="{{ line.supplier.id }}" selected>{{ line.supplier.nom }}</option>
                    {% else %}
                         <option selected></option>
                    {% endif %}
                </select>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="form-group col-12">
            <label>Date de commande{{ fieldsParam|isFieldRequired('dateCommande', 'mustToCreate') ? '*' }}</label>
            <input type="text"
                   class="w-100 form-control data {{ fieldsParam|isFieldRequired('dateCommande', 'mustToCreate') ? 'needed' }} datepicker orderDate"
                   name="orderDate"
                   title="Date de commande"
                   data-date="{{ line.orderDate ? (line.orderDate|date('Y-m-d H:i')) }}">

        </div>
    </div>
    <div class="row">
        <div class="form-group col-12">
            <label>Date attendue{{ fieldsParam|isFieldRequired('dateAttendue', 'mustToCreate') ? '*' }}</label>
            <input type="text"
                   class="w-100 form-control data {{ fieldsParam|isFieldRequired('dateAttendue', 'mustToCreate') ? 'needed' }} datepicker expectedDate"
                   name="expectedDate"
                   title="Date attendue"
                   data-date="{{ line.expectedDate ? (line.expectedDate|date('Y-m-d')) }}">
        </div>
    </div>
</form>
