<form method=""
      class="modal fade"
      id="modalNewVisibilityGroup"
      role="dialog"
      aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered"
         role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    Nouveau groupe de visibilité
                </h5>
                <button type="button" class="close"
                        data-dismiss="modal"
                        aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body modal-input container-fluid">
                {% include 'visibility_group/modals/form.content.html.twig' with {
                    visibilityGroup: {
                        'id': null,
                        'label': '',
                        'description': '',
                        'active': false
                    }
                } %}
            </div>
            <div class="error-msg"></div>
            <div class="modal-footer">
                <button type="button"
                        class="btn btn-secondary"
                        data-dismiss="modal">
                    Annuler
                </button>
                <button type="button"
                        id="submitNewVisibilityGroup"
                        class="btn btn-success data">
                    Enregistrer
                </button>
            </div>
        </div>
    </div>
</form>
