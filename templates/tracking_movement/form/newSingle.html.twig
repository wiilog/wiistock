{% import 'form.html.twig' as form %}

<div class="row">
    <div class="form-group col-12">
        <label for="pack" title="UL" class="wii-field-name">{{ trans('Traçabilité', 'Général', 'Unité logistique', false) }}*</label>
        <input type="text" class="form-control data needed" id="pack" name="pack" data-fill-quantity>
        <div class="warning-message wii-warning-text mt-2" hidden>
            Les unités logistiques scannées ne sont pas réellement présentes sur cet emplacement de prise.
        </div>
    </div>
    <div class="form-group col-12">
        {{ form.select('emplacement',  trans('Traçabilité', 'Général', 'Emplacement', false), true , {
            type: 'location',
        }) }}
    </div>

    {% include 'tracking_movement/form/fieldQuantity.html.twig' %}
    {{ form.hidden('is-mass', 0) }}
</div>
