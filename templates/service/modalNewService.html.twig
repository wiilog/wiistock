<form method="" class="modal fade" id="modalNewService" role="dialog" aria-labelledby="dataModalCenterTitle"
    aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">

            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLongTitle">Nouvelle demande de service</h5>

                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>

            <div class="modal-body modal-input container-fluid">

                <div class="form-group">
                    <label for="Objet">Objet</label>
                    <input type="text" id="libelle" class="form-control data needed" name="Libelle">
                </div>

                <div class="form-group">
                    <label for="demandeur">Demandeur</label>
                    <select type="text" class="demandeur form-control data needed" name="demandeur" disabled>
                        <option value="{{app.user.id}}">{{ app.user.username }}</option>
                    </select>
                </div>

                <div class="form-group needed">
                    <label for="Localité">Source</label>
                    <input class="form-control data needed" name="source">
                </div>
                
                <div class="form-group needed">
                    <label for="Localité">Destination</label>
                    <input class="form-control data needed" name="destination">
                </div>

                <div class="form-group">
                    <label for="commentaire">Commentaire</label>
                    <input id="commentaire" class="commentaire form-control data" name="commentaire" type="hidden">
                    <div class="editor-container">
                    </div>
                </div>
                <div class="error-msg"></div>

            </div>
            <div class="modal-footer">
                <button type="button" class="btn" data-dismiss="modal">Annuler</button>
                <button id="submitNewService" type="button" class="btn btn-success"
                    onclick='setCommentaire($(this))'>Enregistrer</button>
            </div>
        </div>
    </div>
</form>