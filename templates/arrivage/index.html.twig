{% extends 'layout.html.twig' %}

{% block title %}{{ 'arrivage.arrivages'|trans|capitalize }}{% endblock %}

{% block page_filter %}
    {% include 'filters/filtersArrivage.html.twig' with {
        'page': 'PAGE_ARRIVAGE',
        'tableSelector': '#tableArrivages',
        'callback': '() => {clicked = true;}'
    } %}
{% endblock %}

{% block page_content %}
    <input type="hidden" id="dateMinFilter">
    <input type="hidden" id="dateMaxFilter">
    <input type="hidden" id="statutFilter">
    <input type="hidden" id="usersFilter">
    <h1 title="arrivages">{{ 'arrivage.arrivages'|trans|capitalize }}</h1>

    <div class="row">
        {% if hasRight(constant('App\\Entity\\Menu::TRACA'), constant('App\\Entity\\Action::CREATE')) %}
            <a href="" data-toggle="modal" data-target="#modalNewArrivage" class="btn btn-primary"
               data-backdrop="static" data-keyboard="false"
               onclick="initNewArrivageEditor('#modalNewArrivage')">
                <span class="fa fa-plus mr-2"></span>
                <span title="nouvel arrivage">{{ 'arrivage.nouvel arrivage'|trans|capitalize }}</span>
            </a>
        {% endif %}
        {% if hasRight(constant('App\\Entity\\Menu::TRACA'), constant('App\\Entity\\Action::EXPORT')) %}
            <div class="btn btn-primary" onclick="generateCSV('get_arrivages_for_csv', '{{ 'arrivage.arrivages'|trans }}')">
                <i class="fa fa-file-csv mr-2"></i>Exporter au format CSV
            </div>
        {% endif %}
    </div>
    <div class="d-none justify-content-center" id="spinner">
        <div class="spinner-border sizeLoader" role="status">
            <span class="sr-only">Loading...</span>
        </div>
    </div>
    <div class="col-12 mt-5">
        <table id="tableArrivages" class="table table-striped table-bordered w-100">
        </table>
    </div>
    <div hidden id="barcodes"></div>
    <button class="btn" id="cannotGenerate" data-target='#modalCannotGenerate' data-toggle='modal' hidden></button>
    <input id="redirect" value="{{ redirect }}" hidden>
    <input id="pageLengthForArrivage" value="{{ pageLengthForArrivage }}" hidden>

    {% include "arrivage/modalNewArrivage.html.twig" %}
    {% include "arrivage/modalEditArrivage.html.twig" %}
    {% include "arrivage/modalDeleteArrivage.html.twig" %}
    {% include "arrivage/modalListColis.html.twig" %}

    <input hidden id="noArrTranslation" value="{{ "arrivage.n° d'arrivage"|trans|capitalize }}">
    <input hidden id="acheteursTranslation" value="{{ "arrivage.acheteurs"|trans|capitalize }}">
    <input hidden id="destinataireTranslation" value="{{ "arrivage.destinataire"|trans|capitalize }}">
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src = "{{ asset('js/attachments.js') }}?{{ date().timestamp }}"></script>
    <script src = "{{ asset('js/arrivage-common.js') }}?{{ date().timestamp }}"></script>
    <script src = "{{ asset('js/arrivage.js') }}?{{ date().timestamp }}"></script>
{% endblock %}
