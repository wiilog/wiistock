<div class="d-flex">
    <div class="dropdown dropright">
        <a href="#"
           data-toggle="dropdown"
           aria-haspopup="true"
           aria-expanded="false">
            <img alt="Actions" src="{{ asset('svg/dotsblack.svg') }}" height="20px" width="8px"/>
        </a>
        <div class="dropdown-menu dropdown-follow-gt pointer">
            {% if hasRight(constant('App\\Entity\\Menu::QUALI'), constant('App\\Entity\\Action::EDIT')) %}
                <a class="d-none action-on-click"
                   data-toggle='modal'
                   data-target='#modalEditLitige'
                   onclick="editRowLitigeArrivage($(this), openTableHisto, {{ arrivageId }}, {{ disputeId }}, '{{ disputeNumber }}')">
                    Modifier
                </a>
            {% endif %}
            {% if hasRight(constant('App\\Entity\\Menu::QUALI'), constant('App\\Entity\\Action::DELETE')) %}
                <a onclick="deleteRow($(this), $('#modalDeleteLitige'), $('#submitDeleteLitige'))"
                   class="dropdown-item d-flex align-items-center"
                   data-id='{{ disputeId }}' data-target='#modalDeleteLitige' data-toggle='modal'>
                    <span class="wii-icon wii-icon-trash-black mr-2"></span>Supprimer
                </a>
            {% endif %}
        </div>
    </div>
</div>
