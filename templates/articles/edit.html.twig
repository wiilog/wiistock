{% extends 'layout.html.twig' %}

{% block title %}Edit Articles{% endblock %}

{% block page_content %}
    <h1>Modifier article</h1>

    {{ dump(app.request.server.get("HTTP_REFERER")) }}

    {{ include('articles/_form.html.twig', {'button_label': 'Modifier'}) }}
    
    <a href={{path('reception_ajout_article', {'id': id , 'k': 0})}} class="btn pull-left button btn-right">Liste reception</a>
    <a href={{path('articles_index') }} class="btn pull-left button btn-right">Liste article</a>

    {{ include('articles/_delete_form.html.twig') }}
{% endblock %}
