{% extends 'layout.html.twig' %}

{% block title %}Modifier fournisseur{% endblock %}

{% block page_content %}
    <h1>Modifier fournisseur</h1>

    {% if message is defined %}
    <div>
        {{message}}
    </div>
{% endif %}

    {{ include('fournisseur/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('fournisseur_index') }}"class="btn pull-left button btn-left">retour à la liste</a>

    {{ include('fournisseur/_delete_form.html.twig') }}
{% endblock %}