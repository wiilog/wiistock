{% extends 'base.html.twig' %}

{% block title %}CategoriesVehicules index{% endblock %}

{% block body %}
    <h1>CategoriesVehicules index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categories_vehicule in categories_vehicules %}
            <tr>
                <td>{{ categories_vehicule.id }}</td>
                <td>{{ categories_vehicule.nom }}</td>
                <td>
                    <a href="{{ path('categories_vehicules_show', {'id': categories_vehicule.id}) }}">show</a>
                    <a href="{{ path('categories_vehicules_edit', {'id': categories_vehicule.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('categories_vehicules_new') }}">Create new</a>
{% endblock %}
