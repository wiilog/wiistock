{% extends 'base.html.twig' %}

{% block title %}Racks{% endblock %}

{% block body %}
    <h1>Racks</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rack.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ rack.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('racks_index') }}">back to list</a>

    <a href="{{ path('racks_edit', {'id': rack.id}) }}">edit</a>

    {{ include('racks/_delete_form.html.twig') }}
{% endblock %}