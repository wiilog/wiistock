{% extends 'base.html.twig' %}

{% block title %}CommandesClients{% endblock %}

{% block body %}
    <h1>CommandesClients</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commandes_client.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ commandes_client.libelle }}</td>
            </tr>
            <tr>
                <th>Date_commande</th>
                <td>{{ commandes_client.dateCommande ? commandes_client.dateCommande|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commandes_clients_index') }}">back to list</a>

    <a href="{{ path('commandes_clients_edit', {'id': commandes_client.id}) }}">edit</a>

    {{ include('commandes_clients/_delete_form.html.twig') }}
{% endblock %}