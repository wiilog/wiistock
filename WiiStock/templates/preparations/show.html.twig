{% extends 'base.html.twig' %}

{% block title %}Preparations{% endblock %}

{% block body %}
    <h1>Preparations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ preparation.id }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ preparation.statut }}</td>
            </tr>
            <tr>
                <th>Date_preparation</th>
                <td>{{ preparation.datePreparation ? preparation.datePreparation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('preparations_index') }}">back to list</a>

    <a href="{{ path('preparations_edit', {'id': preparation.id}) }}">edit</a>

    {{ include('preparations/_delete_form.html.twig') }}
{% endblock %}