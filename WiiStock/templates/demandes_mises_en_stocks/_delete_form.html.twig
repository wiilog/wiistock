<form method="post" action="{{ path('demandes_mises_en_stocks_delete', {'id': demandes_mises_en_stock.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demandes_mises_en_stock.id) }}">
    <button class="btn">Delete</button>
</form>