<form method="post" action="{{ path('champs_personnalises_delete', {'id': champs_personnalise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ champs_personnalise.id) }}">
    <button class="btn">Delete</button>
</form>