{% extends 'layout.html.twig' %}

{% block title %}Suppression sous-catégorie "{{ sous_categories_vehicule.nom }}"{% endblock %}

{% block page_content %}
    <h1>SUPPRESSION SOUS-CATEGORIE "{{ sous_categories_vehicule.nom }}"</h1>

    <table class="table table-condensed table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Code</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ sous_categories_vehicule.id }}</td>
                <td>{{ sous_categories_vehicule.nom }}</td>
                <td>{{ sous_categories_vehicule.code }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('sous_categories_vehicules/_delete_form.html.twig') }}

    <a href="{{ path ('parc_parametrage') }}" class="button btn-left pull-left">
        <span class="fa fa-angle-double-left"></span>Retour</a>
{% endblock %}