{% extends 'layout.html.twig' %}

{% block title %}Suppression marque "{{ marque.nom }}"{% endblock %}

{% block page_content %}
    <h1>SUPPRESSION MARQUE "{{ marque.nom }}"</h1>

    <table class="table table-condensed table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ marque.id }}</td>
                <td>{{ marque.nom }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('marques/_delete_form.html.twig') }}

    <a href="{{ path ('parc_parametrage') }}" class="button btn-left pull-left">
        <span class="fa fa-angle-double-left"></span>Retour</a>
{% endblock %}
