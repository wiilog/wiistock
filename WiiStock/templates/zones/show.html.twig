{% extends 'base.html.twig' %}

{% block title %}Zones{% endblock %}

{% block body %}
    <h1>Zones</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ zone.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ zone.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('zones_index') }}">back to list</a>

    <a href="{{ path('zones_edit', {'id': zone.id}) }}">edit</a>

    {{ include('zones/_delete_form.html.twig') }}
{% endblock %}