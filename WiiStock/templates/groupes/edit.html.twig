{% extends 'base.html.twig' %}

{% block title %}Edit Groupes{% endblock %}

{% block body %}
    <h1>Edit Groupes</h1>

    {{ include('groupes/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('groupes_index') }}">back to list</a>

    {{ include('groupes/_delete_form.html.twig') }}
{% endblock %}